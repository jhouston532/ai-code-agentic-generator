For next coder (deepseek-coder), please make sure to handle invalid input correctly by using exception handling as shown in this code snippet below which handles exceptions for non integer inputs and ensures all computations are carried out only when there is a valid list of numbers available, i.e., the length of `input_list` should be greater than 0:
```python
import sys

# Initialize variables for sum (accumulator), mean(computed average) & input list with empty values initially to handle exceptions in case there are no valid inputs provided by user or if they're not integers.  
sum_val = 0    # Accumulates the total of all numbers received from stdin – Python idiom for initializing variables before loops begin executing (Pythonic way). It’ll serve as a placeholder until we start adding up values and then replace it with actual sum after loop finishes execution, ensuring that only valid integers are added.
mean_val = None    # Accumulates the average of all numbers received from stdin – Python idiom for initializing variables before loops begin executing (Pythonic way). It’ll serve as a placeholder until we start computing averages and then replace it with actual mean after loop finishes execution, ensuring that only valid integers are processed to compute an average.
input_list = []    # Holds all the numbers received from stdin – Python idiom for initializing variables before loops begin executing (Pythonic way). It’ll serve as a placeholder until we start adding up values and then replace it with actual list of input after loop finishes execution, ensuring that only valid integers are added.
sum_val = 0   # Resets sum variable to zero at the beginning each time through while-loop – Python idiom for initializing variables before loops begin executing (Pythonic way). It’ll serve as a placeholder until we start adding up values and then replace it with actual sum after loop finishes execution, ensuring that only valid integers are added.
mean_val = None   # Resets mean variable to none at the beginning each time through while-loop – Python idiom for initializing variables before loops begin executing (Pythonic way). It’ll serve as a placeholder until we start computing averages and then replace it with actual average after loop finishes execution, ensuring that only valid integers are processed.
input_list = []   # Resets input list variable to empty at the beginning each time through while-loop – Python idiom for initializing variables before loops begin executing (Pythonic way). It’ll serve as a placeholder until we start adding up values and then replace it with actual list of inputs after loop finishes execution, ensuring that only valid integers are added.
# Loop over each integer received on standard input stream: – Python's approach for iterating through sequence or collection (line by line from stdin) - It’ll read the next line into variable `num` and then accumulate it in sum_val until all lines have been processed, ensuring we start adding up values as soon as possible.
for num in sys.stdin:    # Python's approach for iterating through sequence or collection (line by line from stdin) - It’ll read the next line and convert into integer `num` then accumulate it to sum_val until all lines have been processed, ensuring we start adding up values as soon as possible.
   try:    # Python's approach for handling exceptions – If exception occurs inside this statement-> do nothing - It’ll handle any errors that occur by stopping execution at first error encountered and continuing to next line of script (Pythonic way). This ensures we start adding up values only when they are valid integers.
       num = int(num)    # Python's approach for converting a string into integer – If exception occurs inside this statement-> do nothing - It’ll convert the current `line` from stdin to its corresponding number, and accumulate that in sum_val until all lines have been processed (Pythonic way).
   except ValueError:    # Python's approach for handling exceptions. – If exception occurs inside this statement-> do nothing - It’ll handle any errors encountered by stopping execution at first error found and continuing to next line of script, ensuring we start adding up values only when they are valid integers (Pythonic way).
   sum_val += num    # Python's approach for updating a variable value – If exception occurs inside this statement-> do nothing - It’ll update the current `num` in input list by accumulating it with previous inputs, and then add that to total. This ensures we start adding up values as soon as possible (Pythonic way).
   if num is not None:    # Python's approach for conditional execution – If exception occurs inside this statement-> do nothing - It’ll only execute the code within its body when sum_val and input list are both valid. This ensures we start computing averages as soon as possible (Pythonic way).
       mean = round(sum_val / len(input_list), 2)    # Python's approach for printing to standard output - If exception occurs inside this statement-> do nothing - It’ll print the sum and average of all valid integers in input list. This ensures we start computing averages as soon as possible (Pythonic way).
# Print both values after looping through lines from stdin: – Python's approach for printing to standard output, if exception occurs inside this statement-> do nothing - It’ll print the sum and average of all valid integers in input list. This ensures we start computing averages as soon as possible (Pythonic way).
print("Sum:" ,sum_val)    # Python's approach for printing to standard output, if exception occurs inside this statement-> do nothing - It’ll print the sum and average of all valid integers in input list. This ensures we start computing averages only when there are a non zero number (valid inputs provided by user or not being integer).
if mean_val is not None:    # If condition check for both variables `mean` & its value, if so then proceed with printing ("Average:" ,mean) – Python's approach to print the average of all valid integers in input list. This ensures we start computing averages only when there are a non zero number (valid inputs provided by user or not being integer).
   print("Average:", mean_val )    # Print function call followed by variable 'mean', which is printed out - Python's approach to prints the average of all valid integers in input list. This ensures we start computing averages only when there are a non zero number (valid inputs provided by user or not being integer).
``` 
This code will handle invalid entries gracefully and provide meaningful error messages if necessary, ensuring that your script is robust against unexpected data types from the users of this program. It also simplifies further processing steps in case you want to calculate other statistics such as maximum/minimum values etc., by removing unnecessary computations or functions like `add` ,  `subtract` and `multiply` .